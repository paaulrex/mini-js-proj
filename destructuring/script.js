const randomThings = {
    diveLocation: 'Guam',
    activity: 'scuba diving',
    diveSpot: 'Gab Gab 2',
    pointOfInterets: 'nurse shark',
    company: 'diving friends'
}

const {diveLocation, activity, diveSpot, pointOfInterets, company} = randomThings

console.log(`I would like to go back to ${diveLocation} to go ${activity} at ${diveSpot} to see a ${pointOfInterets} with my ${company}`)